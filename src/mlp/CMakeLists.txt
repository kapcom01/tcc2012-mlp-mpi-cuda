CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(ParallelMLP)

SET(SOURCES
	BackpropMLP.cpp
	ExampleSet.cpp
	Layer.cpp
	Neuron.cpp
)

INCLUDE_DIRECTORIES(${CUDA_INCLUDE_DIRS})
ADD_LIBRARY(mlp ${SOURCES})
TARGET_LINK_LIBRARIES(mlp ${CUDA_LIBRARIES})
