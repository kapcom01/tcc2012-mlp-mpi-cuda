CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(ParallelMLP)

SET(SOURCES
	ExampleSet.cu
)

SET(CMAKE_CXX_FLAGS "-O2")
SET(CUDA_NVCC_FLAGS)

INCLUDE_DIRECTORIES(${CUDA_INCLUDE_DIRS})
CUDA_ADD_LIBRARY(mlp_common ${SOURCES})
TARGET_LINK_LIBRARIES(mlp_common ${CUDA_LIBRARIES})
