CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(ParallelMLP)

SET(SOURCES
	Chronometer.cpp
	ExampleSet.cpp
	Layer.cpp
	MLP.cpp
)

INCLUDE_DIRECTORIES(${CUDA_INCLUDE_DIRS})
ADD_LIBRARY(mlp_common ${SOURCES})
TARGET_LINK_LIBRARIES(mlp_common ${CUDA_LIBRARIES} rt)
